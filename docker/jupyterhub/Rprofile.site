# Setting default repo globally
home <- Sys.getenv("R_HOME")
.libPaths(c(file.path(home, "lib"), .libPaths()))

r <- getOption("repos")
r["CRAN"] <- Sys.getenv(
  "CRAN",
  unset = "https://nexus.ssb.no/repository/packagemanager-rstudio-noble/"
)
options(repos = r)
rm(r)
